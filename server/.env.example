# Server
PORT=3000

# Admin key — used to manage devices and API keys via the admin routes
# Change this to a strong random string before deploying
ADMIN_KEY=change-me-before-deploy

# Neon (or any Postgres) connection string
# Get this from your Neon dashboard: Settings → Connection string
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
